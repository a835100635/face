/**
 * @fileoverview 用于测试的 html 内容
 */
export default `<title>富文本示例</title>
<div><pre><code class="language-javascript">function fn(a) {
	if (typeof a === 'number') {
		return a + 100 // comments
	}
	return 0
}</code></pre>`
